@model src.ViewModels.PessoaViewModel

@{
    ViewData["Title"] = "Update";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Alterar cliente</h4>
        <partial name="_Form" />
    </div>
</div>